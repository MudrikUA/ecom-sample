import styles from './ProfileDelete.module.css'

export default function ProfileDelete({auth}) {

    return <div className={styles.profileContent}>
        <p className={styles.formHeader}>Delete Content</p>
        <p>In development...</p>
    </div>
}